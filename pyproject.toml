[project]
name = "restaurant-finder-mcp"
version = "0.1.0"
description = "MCP server for finding restaurants using Google Maps"
requires-python = ">=3.9"
dependencies = [
    "googlemaps>=4.10.0"
]

[project.scripts]
restaurant-finder-mcp = "restaurant_finder_mcp.server:main"
